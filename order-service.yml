 spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_db
  jpa:
      hibernate:
        ddl-auto: update
      properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: true
  sql:
    init:
    mode: always